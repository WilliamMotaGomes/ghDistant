# Partage des hooks Git

## Installation du hook `pre-commit`

1. Copier le fichier `pre-commit` dans le répertoire `.git/hooks/` de votre dépôt local :
   ```bash
   cp hooks/pre-commit .git/hooks/pre-commit